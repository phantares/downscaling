optimizer:
  lr: 0.0001
  betas: [0.9, 0.999]
  eps: 0.00000001
  weight_decay: 0.0005

data_config:
  batch_size: 20
  num_workers: 4

max_epochs: 200

early_stopping:
  patience: 8
  monitor: val_loss