name: gan
config:
  scaling:
    execute: True
    rain:
      method: log_norm
      config:
        eps: 1e-2
discriminator:
  name: discriminator
  weight: 0.015
  config:
    conv:
      kernel_size: 4
      stride: 2
      padding: 1
    output:
      kernel_size: 3
      padding: 0