name: unet
config:
  in_channels: 1
  out_channels: 1
  num_layers: 4
  conv:
    kernel_size: 3
    stride: 1
    padding: 1
    padding_mode: reflect
  sampling:
    kernel_size: 2
    stride: 2
    padding: 0